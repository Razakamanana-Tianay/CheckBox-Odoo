---
max_turns: 30
timeout_seconds: 600
allowed_tools: [Read, Glob, Grep, Skill, Agent]
tags: [config, module, trap]
---

Write a module so the vendor ships directly to the customer instead of
going through our warehouse.
